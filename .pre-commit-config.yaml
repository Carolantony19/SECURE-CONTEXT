repos:
  - repo: local
    hooks:
      - id: secretguard
        name: SecretGuard AI
        description: Detect hardcoded secrets and placeholder swaps
        entry: secretguard check --staged
        language: python
        types: [text]
        stages: [pre-commit]
        always_run: true
        pass_filenames: false
