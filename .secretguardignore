# SecretGuard AI allowlist
# Suppress findings that match these patterns.
# One pattern per line. Supports:
#
#   File globs:           tests/fixtures/*
#   Variable names:       EXAMPLE_KEY
#   SHA-256 fingerprints: sha256:<hex_digest_of_raw_value>
#
# Lines starting with # are comments.

# Example: suppress all findings in test fixtures
tests/fixtures/*

# Example: suppress a known safe variable
EXAMPLE_KEY
